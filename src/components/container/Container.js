import React from "react"

const Button = (props) => {
    return (
        <>
            <div style={{padding: 10}}>
                {props.container}
            </div>
        </>
    )
}

export default Button